ANTHROPIC_API_KEY=sk-ant-...
MORALIS_API_KEY=...
ETHERSCAN_API_KEY=...
CRYPTOPANIC_API_KEY=

# Zerion (wallet balances) — https://developers.zerion.io
ZERION_API_KEY=...

# Dune Sim (transaction history only — balances migrated to Zerion)
DUNE_SIM_API_KEY=...
FRONTEND_URL=http://localhost:5173
PORT=3001

# PostgreSQL (Prisma)
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require

# Wallet snapshot TTL (ms) — on GET wallet / chat / market, refresh from indexer if older than this
WALLET_SNAPSHOT_TTL_MS=3600000

# Min interval between manual UI refreshes per address (ms)
WALLET_REFRESH_COOLDOWN_MS=30000

